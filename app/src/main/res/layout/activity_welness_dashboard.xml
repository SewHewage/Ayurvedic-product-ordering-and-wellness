<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/light_beige">

    <!-- Welcome Message -->
    <TextView
        android:id="@+id/welcomeMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Ayurvedic Wellness Center"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:textColor="@color/brown"
        android:layout_gravity="center"
        android:padding="10dp"
        android:textAlignment="center"
        android:drawableTop="@drawable/logo"
        android:gravity="center_horizontal" />

    <!-- Choices Section -->
    <TextView
        android:id="@+id/chooseRoleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choose Your Role:"
        android:layout_marginTop="25dp"
        android:textSize="20sp"
        android:textColor="@color/ayurvedic_hint"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal">

        <!-- Customer Button -->
        <Button
            android:id="@+id/customerButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginTop="16dp"
            android:background="@drawable/rounded_button"
            android:drawableTop="@drawable/customer_icon"
            android:fontFamily="sans-serif-medium"
            android:gravity="center_horizontal"
            android:text="Customer"
            android:textColor="@color/ayurvedic_button"
            android:textSize="16sp" />

        <!-- Spacer -->
        <Space
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- Wellness Consultant Button -->
        <Button
            android:id="@+id/consultantButton"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="80dp"
            android:background="@drawable/rounded_button"
            android:text="Wellness Consultant"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/ayurvedic_button"
            android:drawableTop="@drawable/consultant_icon"
            android:gravity="center_horizontal" />

        <!-- Spacer -->
        <Space
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- Admin Button -->
        <Button
            android:id="@+id/adminButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="16dp"
            android:background="@drawable/rounded_button"
            android:text="Admin"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/ayurvedic_button"
            android:drawableTop="@drawable/admin_icon"
            android:gravity="center_horizontal" />
    </LinearLayout>
</LinearLayout>
